import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';
// every react application starts with a simple div that's named root. It's in the public index
ReactDOM.render(<App />, document.getElementById('root'));
